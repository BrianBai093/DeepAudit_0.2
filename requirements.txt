# DeepAudit v0.5 dependencies
# ---------------------------
# Core
pydantic>=2.0
openai>=1.0

# Phase 2: Claude Code Agent SDK (recovery + autonomous exploration)
claude-agent-sdk>=0.1.0

# RAG: code embedding + vector search
numpy>=1.24

# Visual extraction + figure reproduction
PyMuPDF>=1.23
matplotlib>=3.7
